"""
Deep learning track: MINiT architecture, training, and experiment utilities.

This sub-package implements the Multiple Instance Neuroimage Transformer
(MINiT) for volumetric MRI classification. All modules require PyTorch,
which is an optional dependency (install with ``pip install -e ".[dl]"``).
"""
